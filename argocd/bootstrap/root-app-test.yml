apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-bootstrap-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: file:///repos/platform-argocd-apps
    targetRevision: HEAD
    path: argocd/bootstrap/test
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
