apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tenant: dq (dq-made-easy)
# Base — namespace and shared labels only.
# Service manifests (Deployment, Service, Ingress) live in the consumer repo
# and are referenced by ArgoCD Applications in overlays/.

commonLabels:
  platform.jaccloud.nl/managed-by: argocd
  platform.jaccloud.nl/tenant: dq

resources:
  - namespace.yml
