---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-maas-central-repo-test
  namespace: argocd
spec:
  project: tenant
  source:
    repoURL: "{{MAAS_REPO_URL}}"
    targetRevision: HEAD
    path: k8s/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: maas-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
