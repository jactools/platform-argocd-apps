apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-trino

commonLabels:
  platform.jaccloud.nl/environment: test

resources:
  - ../../base

patches:
  - target:
      kind: Ingress
      name: trino
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: trino.test.jac.dot
