---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-trino
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: file:///repos/platform-argocd-apps
    targetRevision: release/dev
    path: apps/platform/trino/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-trino
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true