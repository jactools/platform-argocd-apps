---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-keycloak
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: "{{ARGOCD_REPO_URL}}"
    targetRevision: feature/prototype
    path: apps/platform/keycloak/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
