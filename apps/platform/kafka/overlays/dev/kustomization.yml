apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-kafka

commonLabels:
  platform.jaccloud.nl/environment: dev

resources:
  - ../../base

patches:
  - target:
      kind: Ingress
      name: kafka
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: kafka.dev.jac.dot
