apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-ingestion

commonLabels:
  platform.jaccloud.nl/environment: test

resources:
  - ../../base

patches:
  - target:
      kind: Ingress
      name: ingestion-runner
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: ingestion-runner.test.jac.dot
