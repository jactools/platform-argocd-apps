apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Platform service: kong
# Base manifests — service-specific resources go here
# Populated in W3 (Platform service manifests and ArgoCD apps)

commonLabels:
  platform.jaccloud.nl/managed-by: argocd
  platform.jaccloud.nl/service: kong
  platform.jaccloud.nl/tenant: platform

resources: []
